{# ========================================================================== #}
{# :: Macro #}
{# ========================================================================== #}
{% macro color(index) %}
{% set colors = [
    "violet",
    "blue",
    "rose",
    "shade",
] %}
{{ colors[(index - 1) % colors|length] }}
{% endmacro %}