{# ========================================================================== #}
{# :: Macro #}
{# ========================================================================== #}
{% macro color(index) %}
{% set colors = [
    "violet",
    "blue",
    "rose",
    "shades",
] %}
{{ colors[(index - 1) % colors|length] }}
{% endmacro %}