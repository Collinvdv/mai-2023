{# ========================================================================== #}
{# Inheritance #}
{# ========================================================================== #}
{% extends "base/layout" %}

{# ========================================================================== #}
{# Content #}
{# ========================================================================== #}
{% block content %}
     {% cache globally using key "detail-#{entry.slug}" if not craft.app.config.general.devMode  %}
        {# Dynamic blocks #}
        {% include "/sections/_index.twig" %}
    {% endcache %}
{% endblock %}