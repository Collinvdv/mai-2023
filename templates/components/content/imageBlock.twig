

{# ========================================================================== #}
{# :: Variables #}
{# ========================================================================== #}


{# ========================================================================== #}
{# :: Component #}
{# ========================================================================== #}
{% if data.imageField.one() %}
    <img src="{{data.imageField.one().getUrl({
                width: 1200,
                format: 'webp',
                quality: 80,
                mode: 'fit'
            }) }}" class="w-full max-w-none c-content-image">
{% endif %}